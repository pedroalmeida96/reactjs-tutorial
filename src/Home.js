const Home = () => {
  const firstTry = "This is my first try with react.";
  return (
    <div className="home">
      <h2>HOMEPAGE</h2>
      <p>{firstTry}</p>
    </div>
  );
};

export default Home;
